// set initial count
let count = 0

// select value and button, '#' for ids, '.' for classes

const value = document.querySelector('#value')
const btns = document.querySelector('.btn')

btns.forEach(function (item) {
    console.log()
})